export const designingDataIntensiveApplications = {
  slug: 'designing-data-intensive-applications',
  title: 'Designing Data-Intensive Applications',
  author: 'Martin Kleppmann',
  coverUrl: 'https://covers.openlibrary.org/b/isbn/9781449373320-L.jpg',
  accentColor: '#b83c24',
  category: 'Software Architecture',
  rating: 5,
  description: 'The absolute gold standard for understanding system design, databases, distributed networks, and scalability. It teaches how to navigate trade-offs in modern data architectures by looking under the hood of popular storage engines, processing frameworks, and replication protocols.',
  goodreadsUrl: 'https://www.goodreads.com/book/show/23463279-designing-data-intensive-applications',
  notes: [
    'System Foundations — Reliability, Scalability, & Maintainability: The core qualities of a robust data system depend on handling load, fault tolerance, and code simplicity. Under heavy loads, system faults must not escalate to full failures.\n- Reliability: A system works correctly (performs expected functions at desired performance) even in the face of adversity (hardware faults, software bugs, human errors). Faults are defined as one component deviating from its spec, whereas failure is when the entire system stops.\n- Scalability: The system\'s ability to cope with increased load. Describe load using load parameters (e.g., requests per second, write-to-read ratio, active users). For example, Twitter handles timeline generation by caching home timelines (fan-out approach) or querying active followers, constantly balancing write-intensive vs read-intensive strategies.\n- Latency Percentiles: Do not rely on average response times (mean) to measure performance. Use percentiles (p95, p99, p99.9) to track tail latency, representing the slowest requests that often affect your most active or high-value users.\n- Maintainability: Design for change. Keep complexity low (simplicity via clean abstractions), make operations easy (operability), and write code that can be evolved over time (evolvability).',

    'Data Representation — Models and Query Languages: How data is structured determines how we think about the application. Relational databases, document stores, and graph models each solve distinct structural challenges.\n- Impedance Mismatch: The disconnect between application code (object-oriented model) and database structure (tables, rows, columns) is resolved differently. Relational tables require mapping layers (ORMs), whereas document databases (MongoDB) store data closer to the application\'s structure but sacrifice clean joins.\n- Declarative vs Imperative: SQL is declarative; you specify *what* data you want, and the query optimizer decides *how* to retrieve it. Imperative languages specify *how* to perform operations (like looping). Declarative queries permit automatic optimization and parallel execution without rewrite.\n- Schemas: Document databases are often called "schema-less," but a more accurate term is "schema-on-read" (data is unstructured but interpreted upon reading). Relational databases use "schema-on-write" (strict validation of data structure before storage).',

    'Storage Engines — LSM-Trees vs B-Trees: The storage engine at the heart of every database dictates read and write performance, selecting either log-structured or page-oriented structures.\n- LSM-Trees: Log-Structured Merge-Trees write data sequentially. Writes go to an in-memory balanced tree (MemTable) and are periodically flushed to disk as immutable Sorted String Tables (SSTables). Compaction merges and discards duplicate keys in the background. Highly optimized for high-write throughput.\n- B-Trees: The traditional standard for databases. They break database files down into fixed-size pages (typically 4KB) and write in-place. A Write-Ahead Log (WAL) is used to recover from crashes. Highly optimized for read performance but suffers from write amplification and fragmentation.\n- Column-Oriented Storage: In data warehousing (OLAP), storing data by columns rather than rows drastically speeds up analytics. It enables high compression ratios and avoids reading unnecessary data from disk.',

    'Concurrency Controls — Transaction Isolation Levels: Transactions are an abstraction layer that simplifies error handling by grouping multiple reads and writes into a single logical unit.\n- ACID Abstraction: Atomicity (abortability), Consistency (maintaining invariants, application-defined), Isolation (concurrent transactions don\'t interfere), and Durability (data survives crash).\n- Race Conditions: Concurrency bugs cause catastrophic system states. Read skew occurs when a transaction reads inconsistent data at different points in time. Write skew is a generalization of dirty writes where two transactions read the same data, make independent decisions, and write conflicting updates.\n- Read Committed: The basic isolation level. Prevents dirty reads (reading uncommitted writes) and dirty writes (overwriting uncommitted writes) using row-level locks and read-snapshots.\n- Snapshot Isolation: Solves read skew. Every transaction reads from a consistent snapshot of the database from the moment the transaction started. Implemented using Multi-Version Concurrency Control (MVCC).\n- Serializability: The strongest level, guaranteeing that concurrent transactions yield the same result as if run one-by-one. Achieved via Single-Thread Execution, Two-Phase Locking (2PL), or Serializable Snapshot Isolation (SSI)—an optimistic concurrency control.',

    'Distributed Data — Replication and Partitioning: Moving from a single database node to a distributed network increases availability but introduces synchronization and routing complexities.\n- Replication Models: Single-leader (writes go to leader, read from followers), multi-leader (multiple data centers), or leaderless (Dynamo-style, client writes to multiple nodes, resolved using quorums).\n- Replication Lag Anomalies: Asynchronous replication is fast but causes temporary inconsistencies. Read-after-write consistency ensures users always see their own edits. Monotonic reads guarantee users don\'t see time run backward. Consistent prefix reads ensure causal relationships in data are preserved.\n- Partitioning (Sharding): Breaking large datasets into smaller chunks. Can partition by key range (risk of hot spots) or key hash (even distribution). Secondary indexes are harder to handle: local indexes (document-partitioned, require scatter-gather queries) vs global indexes (term-partitioned, write-heavy but fast reads).',

    'Network Fallibility — Distributed Pitfalls and Consensus: Distributed systems are fundamentally different because there is no shared memory, physical clocks drift, and networks can drop packets.\n- Unreliable Clocks: Physical clocks (using NTP) drift, meaning you cannot rely on system time for ordering events (e.g., Last-Write-Wins can delete data). Use logical clocks (Lamport, Vector clocks) to capture causality.\n- Consensus: The core building block of distributed coordination. Nodes must agree on a value (e.g., electing a leader or committing a transaction). Protocols like Raft and Paxos solve consensus under network partition.\n- Two-Phase Commit (2PC): An atomic commitment protocol ensuring all participating nodes either commit or abort. However, it is a blocking protocol; if the coordinator crashes mid-process, participants are left hanging.'
  ],
  finalThoughts: 'Designing Data-Intensive Applications is the definitive guide to modern database and distributed system design. It teaches engineers to look past vendor marketing and understand the real engineering trade-offs between write throughput, read latency, data safety, and network partition tolerance. A masterpiece that transforms backend development from trial-and-error to a disciplined engineering craft.'
};
